{
  "name": "brain-cache",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.9",
    "@testing-library/react": "^11.2.5",
    "@testing-library/user-event": "^12.6.3",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "4.0.1",
    "svg-inline-react": "^3.2.0",
    "web-vitals": "^0.2.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "./scripts/build.sh",
    "develop": "react-app-rewired build && mv build/index.html build/popup.html",
    "test": "react-scripts test",
    "test:output": "react-scripts test --verbose=false --silent=false --watchAll=false",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@svgr/cli": "^5.5.0",
    "react-app-rewired": "^2.1.8",
    "rewire": "^5.0.0",
    "svg-inline-loader": "^0.8.2",
    "webextension-polyfill": "^0.7.0"
  }
}
